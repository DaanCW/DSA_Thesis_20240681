# Dependencies (regenerated by: npm install)
node_modules/

# Hardhat / zkSync compile & deploy output
artifacts/
artifacts-zk/
cache/
cache-zk/
deployments-zk/

# Circuit build output — ptau, proving key, r1cs, wasm (regenerated by: setup.sh)
build/

# Secrets — never commit
.env

# Jupyter checkpoints
.ipynb_checkpoints/

# OS junk
.DS_Store